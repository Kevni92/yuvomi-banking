# Caddy and both upstream containers must share the external proxy network.

yuvomi.example.com {
  handle /api/extensions/banking/* {
    reverse_proxy yuvomi-banking:3100
  }

  handle {
    reverse_proxy yuvomi:3000
  }
}
