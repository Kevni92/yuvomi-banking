# Caddy runs on the host in this example. If Caddy itself runs in a container,
# replace 127.0.0.1 with a shared Docker-network service name.

yuvomi.example.com {
  handle /api/extensions/banking/* {
    reverse_proxy 127.0.0.1:3100
  }

  handle {
    reverse_proxy 127.0.0.1:3000
  }
}
